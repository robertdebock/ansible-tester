#!/usr/bin/env ansible-playbook
---
- name: Converge
  hosts: all
  gather_facts: no
  become: yes

  vars:
    go_packages:
      - github.com/golang/dep

  roles:
    - robertdebock.bootstrap
    - robertdebock.buildtools
    - robertdebock.go
    - robertdebock.git
    - robertdebock.vuls
