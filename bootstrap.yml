#!/usr/bin/env ansible-playbook
---
- name: Converge
  hosts: all:!openbsd
  gather_facts: no
  become: yes
  serial: 33%

  roles:
    - robertdebock.bootstrap

  tasks:
    - name: test connection
      ping:
